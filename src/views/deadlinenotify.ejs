<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Task Deadline Notification</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f4f4f4;
        padding: 20px;
      }
      .container {
        background-color: white;
        border-radius: 8px;
        padding: 20px;
        max-width: 600px;
        margin: auto;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      }
      .header {
        text-align: center;
        padding-bottom: 20px;
      }
      .header h1 {
        color: #333;
      }
      .content {
        line-height: 1.6;
        color: #555;
      }
      .task-list {
        margin-top: 20px;
      }
      .task-list ul {
        list-style-type: none;
        padding-left: 0;
      }
      .task-list ul li {
        background: #f9f9f9;
        margin-bottom: 10px;
        padding: 10px;
        border-left: 5px solid #007bff;
      }
      .task-title {
        font-weight: bold;
        color: #333;
      }
      .task-deadline {
        color: #e74c3c;
      }
      .footer {
        text-align: center;
        margin-top: 20px;
        font-size: 12px;
        color: #888;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1>Deadline Reminder</h1>
      </div>
      <div class="content">
        <p>Hello <%= user.username %>,</p>
        <p>
          This is a friendly reminder that you have some upcoming tasks with
          approaching deadlines:
        </p>
        <div class="task-list">
          <ul>
            <!-- <% task.forEach(function(task) { %>
                        <li>
                            <p class="task-title"><%= task.title %></p>
                            <p>Deadline: <span class="task-deadline"><%= task.dueDate %></span></p>
                        </li>
                    <% }); %> -->
          </ul>
        </div>
        <p>Please make sure to complete them before the deadlines.</p>
        <p>Best regards,<br />Your To-Do List App Team</p>
      </div>
      <div class="footer">
        <p>&copy; 2024 To-Do List App. All rights reserved.</p>
      </div>
    </div>
  </body>
</html>
